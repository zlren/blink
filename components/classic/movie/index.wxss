/* components/classic/movie/index.wxss */

.classic-container {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.classic-img {
  width: 750rpx;
  height: 500rpx;
}

.tag {
  width: 46rpx;
  height: 142rpx;
  /* 相对当前路径进行调整 */
  position: relative;
  bottom: 58rpx;
  right: 310rpx;
}

.content {
  /* display:block;*/
  /* 自适应宽度 */
  max-width: 550rpx;
  font-size: 36rpx;
}
